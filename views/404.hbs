<!DOCTYPE html>
<html lang="es" dir="ltr">
    <head>
        {{>analytics}}
        <meta charset="utf-8">
        <title>Pared</title>
        {{>links}}
    </head>
    <body>
        {{>header}}
        <p>{{description}}</p>
        {{>footer}}
    </body>
</html>
